# Rules

## Правила работы с проектом

### Только для чтения — никогда не редактировать

- `.env`

### Не читать без явного запроса

- `venv/` — виртуальное окружение, не относится к коду проекта
- все файлы из `.gitignore`

### Терминал

- Можно работать в терминале без отдельного запроса пользователя, но только в рамках папки проекта.
- Все команды выполнять только в активированном `venv` проекта.
- Нельзя выполнять `pip install`, `poetry add`, `uv add`, `npm install` и любые другие команды установки зависимостей.
- Если требуется новая зависимость — выдать команду для установки и остановиться.
- Нельзя выходить за пределы проекта.
- Нельзя трогать `.env`, `settings.json`, `venv/`.
- Деструктивные команды выполнять только после явного подтверждения пользователя.
- Разрешены CRUD-операции с файлами и папками проекта, кроме файлов и директорий из `.gitignore`.

### Проверка кода

После изменений обязательно выполнять:

```bash
ruff check .
ruff check . --fix
```

Если найдены ошибки — исправить их.

важно:

- работаем в виртуальном окружении
- пакеты не устанавливайте - только даете команды, я сам
- синтаксис проверяйте ruff check .

Вместо запуска тестов указывать команду для пользователя. Пример:

```bash
pytest tests\test_article_generator_step2_service.py -q
```

или другую конкретную команду для соответствующего теста.

Пользователь самостоятельно запускает тесты и передает результат в чат.

### Редактирование текстовых документов

- документы с кириллицей / кодировка всегда UTF8

### Архитектура

- Работать через service layer.
- Не обходить service layer.
- Не писать SQL напрямую.
- Использовать repository layer.
- Не смешивать API, worker и service слои.

### Observability

- Для трассировки pipeline'ов использовать [Logfire](https://pydantic.dev/logfire).
- В Logfire 4.x режим вывода задаётся через `send_to_logfire`:
  - `"if-token-present"` (дефолт) — без токена только stdout (Rich-формат), с токеном — Logfire Cloud;
  - `False` — только stdout, всегда;
  - `True` — всегда в облако (упадёт без токена).
- Локального web-UI в Logfire 4.x **нет** (команда `logfire web` отсутствует). Для UI реальные варианты:
  - `uv run logfire auth` или `logfire auth` (без `login`! — это не подкоманда, `logfire auth --help` врёт и показывает только `logout`) → Logfire Cloud free tier (https://logfire.pydantic.dev);
  - OTLP-бэкенд (SigNoz, Jaeger, Honeycomb) через `OTEL_EXPORTER_OTLP_ENDPOINT`;
  - прямое задание `LOGFIRE_TOKEN` через env (если токен уже есть).
- ⚠️ После `logfire auth` проект **не создаётся автоматически**. Первый запуск pipeline упадёт молча (stdout работает, cloud — нет). Обязательно: `logfire projects new <name>`.
- ⚠️ `--region` и `--base-url` — ГЛОБАЛЬНЫЕ флаги (перед подкомандой: `logfire --region us auth`, не `logfire auth --region us`).
- Авто-инструментация предпочтительна: `logfire.instrument_openai()`, `logfire.instrument_pydantic()` и т.д.
- Для кастомных шагов — `with logfire.span("name", **attrs)`.
- Не передавать секреты (API-ключи, токены, пароли) в атрибуты span и `logfire.info(...)`. Pydantic-модели с такими полями маскировать через `logfire.obfuscate`.
- Если проект уже использует Langfuse — это другой инструмент, не дублировать; для MVP выбирать один.

### Контракты

Без явного указания пользователя запрещено:

- менять структуру БД;
- менять API-контракты;
- менять state machine;
- менять публичные интерфейсы сервисов.

### Область задачи

- Изменять только файлы, относящиеся к задаче.
- Не выполнять рефакторинг вне задачи без запроса.
- Не исправлять несвязанные проблемы "заодно".

### Поведение

- Если решение неочевидно — сначала предложить план.
- Предпочитать простое решение сложному.
- Не читать и не модифицировать код вне области задачи без подтверждения.

---

## Инженерные принципы

Перед написанием нового кода всегда проверить:

1. Решена ли задача существующим кодом проекта.
2. Есть ли решение во фреймворке.
3. Есть ли решение в стандартной библиотеке Python.
4. Есть ли решение в уже используемых зависимостях.
5. Нужно ли вообще писать новый код.

### Предпочтения

Предпочитать:

- существующий код вместо нового;
- стандартную библиотеку вместо новой зависимости;
- конфигурацию вместо реализации;
- удаление кода вместо добавления;
- простые функции вместо лишних абстракций;
- композицию вместо наследования.

### Избегать

- overengineering;
- premature abstraction;
- helper-классов для одноразового использования;
- generic manager;
- generic service;
- wrapper вокруг wrapper;
- абстракций без нескольких реальных сценариев использования;
- создания файлов "на будущее".

### Python

Предпочитать:

- `pathlib` вместо `os.path`;
- `dataclass` вместо шаблонного кода;
- стандартный `logging`;
- context manager вместо ручного управления ресурсами;
- встроенные типы и коллекции Python.

Перед созданием:

- utility-модуля;
- helper-класса;
- базового класса;
- дополнительного слоя абстракции;
- новой иерархии исключений;

сначала проверить, действительно ли это необходимо.

### Стоимость сопровождения

Считать, что любой новый код имеет стоимость сопровождения.

Порядок предпочтения:

1. Существующий код.
2. Удаление кода.
3. Конфигурация.
4. Стандартная библиотека.
5. Уже используемая зависимость.
6. Новый код.

Новый код — крайний вариант, а не первый.
